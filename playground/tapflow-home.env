# The dev relay's install dir is this folder, not the ~/.tapflow a real install on this Mac uses.
# Loaded with `tsx --env-file=` rather than a `VAR=value` prefix in package.json, which Windows'
# cmd.exe cannot run. A TAPFLOW_HOME already set in your shell still wins.
TAPFLOW_HOME=.
