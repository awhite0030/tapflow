---
"tapflow": minor
---

Support `TAPFLOW_NONINTERACTIVE` environment variable to bypass interactive prompts in CLI commands, particularly when running over ssh or in a pty where standard detection fails.

<!-- changelog: internal — reason -->
